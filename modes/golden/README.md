# golden

Componentes controlados y deterministas: solo fixtures estáticos de `argos-contracts-scenarios/fixtures/{smoke,validation}/`. Sin red, sin servicios reales. Es el modo por defecto de `suites/c06`, `c07`, `c08`, `argos-cyb-01`, `integration` y `regression`.
