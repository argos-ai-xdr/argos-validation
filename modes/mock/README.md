# mock

Dependencias externas simuladas (p. ej. un MISP/CTI de laboratorio en vez del real, un LLM determinista en vez de vLLM). Pendiente de implementar: requiere que `argos-platform`/`argos-core` expongan un modo mock real primero (ARG-015 en adelante). Hasta entonces, ninguna suite de `../../suites/` declara `mode: mock`.
