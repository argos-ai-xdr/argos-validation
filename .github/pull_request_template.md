## Historia

Enlaza la historia `ARG-###` (obligatorio): closes ARG-

## Qué cambia y por qué

## Checklist

- [ ] `make validate` y `make test` pasan localmente.
- [ ] Ningún evaluador nuevo tiene efectos secundarios ni accede a red fuera de `modes/real/`.
- [ ] Si el PR cambia un umbral en `thresholds/`, incluye la justificación y no relaja un gate crítico sin excepción aprobada.
- [ ] `fixtures/validation/` de `argos-contracts-scenarios` no se usó para ajustar este cambio.

## Evidencia / cómo se validó
